/**
 * @param {string} s
 * @return {number}
 */

function firstUniqChar(s) {
    for (let i = 0; i < s.length; i++) {
        if (s.indexOf(s[i]) == i && s.indexOf(s[i], i+1) == -1) { 
          return i;
        } else if (s.length == 1) {
          return 0;
        } 
    }
    return -1;
};
